class myClass {
    public int[] sum(int[] A, int[] B){
        int[] C;
        C = new int[A.length];
        int i;
        i = 0;
        while(i<A.length){
            C[i] = A[i] + B[i];
            i = i + 1;
        }
        return C;
    }
}